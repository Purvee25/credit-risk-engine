# Credit Risk Decision Engine — Python dependencies
# Pinned to versions verified working on Python 3.13.
# Install:  python3 -m pip install -r requirements.txt
#
# Note (macOS): xgboost needs the OpenMP runtime. If import fails with
# "libomp.dylib could not be loaded", run:  brew install libomp

pandas==2.3.1
numpy==2.3.5
scikit-learn==1.7.1
xgboost==3.3.0
shap==0.52.0
matplotlib==3.10.5
streamlit==1.54.0
altair==6.0.0
joblib==1.5.1
SQLAlchemy==2.0.49

# Backend API (server.py) — live scoring for the 3D web app
fastapi==0.139.0
uvicorn==0.51.0
python-multipart==0.0.32  # >=0.0.31 fixes PYSEC-2026-3040 et al.
